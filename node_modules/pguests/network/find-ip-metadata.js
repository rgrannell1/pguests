
"use strict"




const dns   = require('dns')





const findIpMetadata = (ipData, callback) => {
	dns.lookupService(ipData.ip, parseInt(ipData.port), (err, hostname, service) => {
		callback(err, hostname, service)
	})
}





module.exports = findIpMetadata
